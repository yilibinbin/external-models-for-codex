name: Claude for Codex Review

on:
  pull_request:
  workflow_dispatch:

# Security note:
# This workflow intentionally avoids the privileged target-style PR trigger.
# Fork pull requests skip Claude, comments, and annotations by default so secrets
# are never exposed to untrusted fork code.

permissions:
  contents: read
  pull-requests: write
{{CHECKS_PERMISSION}}

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: {{TIMEOUT_MINUTES}}
    env:
      BASE_SHA: ${{ github.event.pull_request.base.sha }}
      HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
      BASE_REPO: ${{ github.repository }}
      IS_FORK: ${{ github.event.pull_request.head.repo.fork }}
      CLAUDE_FOR_CODEX_MODEL: "{{MODEL}}"
      CLAUDE_FOR_CODEX_EFFORT: "{{EFFORT}}"
    steps:
      - name: Checkout pull request
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect fork safety
        id: fork-safety
        shell: bash
        run: |
          set -euo pipefail
          echo "Base SHA: $BASE_SHA"
          if [ "$IS_FORK" = "true" ] || [ "$HEAD_REPO" != "$BASE_REPO" ]; then
            echo "safe_to_review=false" >> "$GITHUB_OUTPUT"
            echo "Claude review skipped for fork pull request." > claude-for-codex-review.md
            printf '{"verdict":"skipped","summary":"Fork pull request skipped by default.","findings":[]}\n' > claude-for-codex-review.json
          else
            echo "safe_to_review=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Codex and Claude for Codex
        if: steps.fork-safety.outputs.safe_to_review == 'true'
        shell: bash
        run: |
          set -euo pipefail
          npm install -g @openai/codex
          codex plugin marketplace add yilibinbin/external-models-for-codex --ref {{RELEASE_REF}}
          codex plugin add claude-for-codex@external-models-for-codex
          CLAUDE_PLUGIN_ROOT="$(
            codex plugin list --json | node -e '
              const fs = require("fs");
              const payload = JSON.parse(fs.readFileSync(0, "utf8"));
              const plugin = (payload.installed || []).find((item) =>
                item.pluginId === "claude-for-codex@external-models-for-codex" ||
                (item.name === "claude-for-codex" && item.marketplaceName === "external-models-for-codex")
              );
              const sourcePath = plugin && plugin.source && plugin.source.path;
              if (!sourcePath) process.exit(1);
              process.stdout.write(sourcePath);
            '
          )"
          if [ ! -f "$CLAUDE_PLUGIN_ROOT/scripts/claude-companion.mjs" ]; then
            echo "Unable to locate installed Claude for Codex runtime." >&2
            exit 1
          fi
          echo "CLAUDE_PLUGIN_ROOT=$CLAUDE_PLUGIN_ROOT" >> "$GITHUB_ENV"

      - name: Run Claude review
        if: steps.fork-safety.outputs.safe_to_review == 'true'
        shell: bash
        run: |
          set -euo pipefail
          MODEL_ARGS=()
          if [ -n "${CLAUDE_FOR_CODEX_MODEL:-}" ]; then
            MODEL_ARGS+=(--model "$CLAUDE_FOR_CODEX_MODEL")
          fi
          if [ -n "${CLAUDE_FOR_CODEX_EFFORT:-}" ]; then
            MODEL_ARGS+=(--effort "$CLAUDE_FOR_CODEX_EFFORT")
          fi
          node "$CLAUDE_PLUGIN_ROOT/scripts/claude-companion.mjs" {{REVIEW_COMMAND}} --json {{QUALITY_ARGS}} --scope branch --base "$BASE_SHA" ${MODEL_ARGS[@]+"${MODEL_ARGS[@]}"} {{ROLE_ARGS}} {{SEMANTIC_ARGS}} > claude-for-codex-review.json

      - name: Render review comment
        if: steps.fork-safety.outputs.safe_to_review == 'true'
        shell: bash
        run: |
          set -euo pipefail
          node "$CLAUDE_PLUGIN_ROOT/scripts/claude-companion.mjs" github-actions render-comment --input claude-for-codex-review.json > claude-for-codex-review.md

      - name: Publish PR comment
        if: steps.fork-safety.outputs.safe_to_review == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- claude-for-codex-review -->';
            const body = `${marker}\n${fs.readFileSync('claude-for-codex-review.md', 'utf8')}`;
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
            async function withBackoff(fn) {
              let lastError;
              for (const delay of [0, 1000, 3000]) {
                if (delay) await sleep(delay);
                try {
                  return await fn();
                } catch (error) {
                  lastError = error;
                  if (![403, 429].includes(error.status)) throw error;
                }
              }
              throw lastError;
            }
            await withBackoff(async () => {
              const comments = await github.paginate(github.rest.issues.listComments, { owner, repo, issue_number, per_page: 100 });
              const existing = comments.find((comment) => comment.user.type === 'Bot' && comment.body && comment.body.includes(marker));
              if (existing) {
                await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
              } else {
                await github.rest.issues.createComment({ owner, repo, issue_number, body });
              }
            });
{{ANNOTATION_STEP}}

      - name: Upload structured review artifact
        uses: actions/upload-artifact@v4
        with:
          name: claude-for-codex-review
          path: |
            claude-for-codex-review.json
            claude-for-codex-review.md
          retention-days: 5
